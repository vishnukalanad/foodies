
export const metadata = {
    title: 'Meals Slug',
    description: 'Meals Slug',
    keywords: 'Meals Slug',
}

export default function MealsSlugLayout({ children }) {
  return <div>{children}</div>;
}